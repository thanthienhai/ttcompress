"""ttcompress — Position-Calibrated Selection (PCS) research code.

Standalone, academic-scope implementation of PCS_METHOD_SPEC.md. Pieces
copied/adapted from the sibling `vncompress` project are noted per-file;
copied verbatim where correctness (e.g. TruncationCompressor's tie-break,
the document-clustered bootstrap) matters more than DRY across repos.
"""
